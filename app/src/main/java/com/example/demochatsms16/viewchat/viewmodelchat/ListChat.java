package com.example.demochatsms16.viewchat.viewmodelchat;

import androidx.lifecycle.LiveData;

import org.jetbrains.annotations.NotNull;

public interface ListChat {
    void listChat(@NotNull LiveData liveData);
}
